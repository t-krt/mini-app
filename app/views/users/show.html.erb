<div class="contents row" >
  <p><%= @name %>さんの投稿一覧</p>
  <% @posts.each do |post| %>
   <%= render partial: "posts/post", locals: { post: post } %>
  <% end %>
  <%# <%= paginate(@post) %> %>
</div>
