.contents-row
  = form_for(@post) do |f|
    %p.input-field.col.s6
      -# = f.label(:title, "タイトル")
      = f.text_field :title, placeholder: "title", class: "validate"
    %p.input-field.col.s12
      -# = f.label(:post, "本文")
      = f.text_area :post, placeholder: "Please enter contents", class: "materialize-textarea"
    %p.btn.waves-effect.waves-light
      %i.material-icons.left
        send
      = f.submit "投稿する"